# .env.example - Local environment variables template
# NOTE: Real secrets are never committed to git. Load them at runtime via Azure Key Vault.

# --- AZURE KEY VAULT CONFIGURATION ---
AZURE_KEYVAULT_NAME="dg-pilot-kv"
AZURE_TENANT_ID="your-azure-tenant-id"
AZURE_CLIENT_ID="your-service-principal-client-id"
AZURE_CLIENT_SECRET="your-service-principal-client-secret"

# --- LOCAL DATABASE & SERVICES (PULLED FROM KEY VAULT AT RUNTIME) ---
# The following keys are stored in the Key Vault and loaded dynamically:
# - DATABASE_URL (e.g., postgresql://user:password@host:5432/dbname)
# - AI_PROVIDER_API_KEY (e.g., OpenAI/Anthropic keys if using cloud APIs)

# --- LOCAL AI SERVICES CONFIGURATION (OLLAMA + QDRANT) ---
OLLAMA_HOST="http://localhost:11434"
QDRANT_HOST="http://localhost:6333"
EMBEDDING_MODEL="nomic-embed-text"
